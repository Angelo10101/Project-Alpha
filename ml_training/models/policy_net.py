"""
Optional custom PyTorch network architecture for the RL policy.

Only needed if you want something other than Stable-Baselines3's default
MlpPolicy (e.g. a custom feature extractor for stacked raycast inputs,
or a recurrent policy for mobs that need memory).
"""

# import torch
# import torch.nn as nn

# TODO: define custom nn.Module here, if needed
